.DS_Store
.env
.env.local
.env.*
!.env.example
.runtime/
.venv/
.venv-test/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
artifacts/
output/
logs/
audits/
.playwright-cli/
.audit-reports/backups/
.distillation_private/

# Local implementation plans (agent/session working notes; not for public repo)
docs/plans/

# Distillation corpus & LLM outputs (local only; schemas stay in repo)
data/distillation/**
!data/distillation/schemas/
!data/distillation/schemas/**
data/distillation/.prepare_source.lock

# Douyin OCR pipeline: local downloaded frames only
douyin_plova/images/

# Methodology book distillation: private sources, runtime state & temp (local only)
.methodology_private/
config/staging/
tmp/

# Methodology distilled corpora & review data (local only; schemas stay in repo)
data/methodology_books/**
!data/methodology_books/schemas/
!data/methodology_books/schemas/**
data/methodology_unified/
data/methodology_sources/

# A/B prose-craft verification run artifacts (logs, not source)
scripts/_ab2_run.log
scripts/_ab*_run.log
# Scene-grounding / material A/B generated draft dumps (scratch data, not source)
scripts/_*_drafts.json
scripts/_ab2_*.json
docker-compose.override.yml

# Local agent/session state and disposable tool environments.
# Keep project skills and source-controlled configuration outside these paths.
.workbuddy/
.omx/
.uv-cache/
.venv-check/
.venv-selftest/
.claude/settings.local.json
.claude/worktrees/
.claude/checkpoints/
.claude/mailbox/
.claude/agent-registry.json
.claude/agent-memory-local/
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
.claude/routines/.state/
.docker-build-stamp
.benchmark/
